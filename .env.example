# Environment variables for Pokemon Team Generator

# Optional: Set a custom port (defaults to 3000)
# PORT=3000

# Note: OpenAI API key is provided by users through the web interface
# This ensures security and allows users to use their own API keys